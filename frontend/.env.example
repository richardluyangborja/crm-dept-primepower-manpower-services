# Copy to .env — never commit real secrets.
VITE_API_URL=http://localhost:8000/api/v1
VITE_APP_NAME=PrimePower CRM
# v1 scaffold flags (specs/16): keep false until Agent H enforces.
VITE_SESSION_TIMEOUT_ENABLED=false
